# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - APP_NAME=samplenode-heroku
    - FOO=FUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - ls -al
    - git push -f git@heroku.com:$APP_NAME.git master
